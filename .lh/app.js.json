{
    "sourceFile": "app.js",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 6,
            "patches": [
                {
                    "date": 1765159604317,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1765160264771,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,9 +1,9 @@\n const express = require(\"express\");\r\n const path = require(\"path\");\r\n const app = express();\r\n \r\n-app.use(express.static(__dirname));  // เสิร์ฟทุกไฟล์ในโฟลเดอร์นี้\r\n+app.use(express.static(__dirname));  // run  \" node app.js \" first\r\n \r\n app.get(\"/\", (req, res) => {\r\n   res.sendFile(path.join(__dirname, \"index.html\"));\r\n });\r\n"
                },
                {
                    "date": 1765178886748,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,11 +1,77 @@\n const express = require(\"express\");\r\n const path = require(\"path\");\r\n+const bodyParser = require(\"body-parser\");\r\n+const bcrypt = require(\"bcrypt\");\r\n+const sqlite3 = require(\"sqlite3\").verbose();\r\n+\r\n const app = express();\r\n+const PORT = 3000;\r\n \r\n-app.use(express.static(__dirname));  // run  \" node app.js \" first\r\n+// สำหรับอ่าน JSON จาก request body\r\n+app.use(bodyParser.json());\r\n+app.use(bodyParser.urlencoded({ extended: true }));\r\n \r\n+// serve static files\r\n+app.use(express.static(__dirname));\r\n+\r\n+// database\r\n+const db = new sqlite3.Database('./thesis.db', (err) => {\r\n+    if(err) console.error(err);\r\n+    else console.log('Connected to SQLite DB');\r\n+});\r\n+\r\n+// สร้าง table Users\r\n+db.run(`CREATE TABLE IF NOT EXISTS Users (\r\n+    id INTEGER PRIMARY KEY AUTOINCREMENT,\r\n+    username TEXT UNIQUE,\r\n+    passwordHash TEXT,\r\n+    fullname TEXT\r\n+)`);\r\n+\r\n+// หน้า index\r\n app.get(\"/\", (req, res) => {\r\n-  res.sendFile(path.join(__dirname, \"index.html\"));\r\n+    res.sendFile(path.join(__dirname, \"index.html\"));\r\n });\r\n \r\n-app.listen(3000, () => console.log(\"Server running on http://localhost:3000\"));\r\n+// API register\r\n+app.post(\"/register\", async (req, res) => {\r\n+    const { username, password, fullname } = req.body;\r\n+\r\n+    if(!username || !password || !fullname) {\r\n+        return res.json({ success: false, message: \"กรอกข้อมูลไม่ครบ\" });\r\n+    }\r\n+\r\n+    const passwordHash = await bcrypt.hash(password, 10);\r\n+\r\n+    db.run(\r\n+        `INSERT INTO Users (username, passwordHash, fullname) VALUES (?, ?, ?)`,\r\n+        [username, passwordHash, fullname],\r\n+        function(err) {\r\n+            if(err) {\r\n+                if(err.message.includes(\"UNIQUE\")) {\r\n+                    return res.json({ success: false, message: \"Username มีคนใช้แล้ว\" });\r\n+                }\r\n+                return res.json({ success: false, message: err.message });\r\n+            }\r\n+            res.json({ success: true, message: \"สมัครสมาชิกเรียบร้อย\" });\r\n+        }\r\n+    );\r\n+});\r\n+\r\n+// API login\r\n+app.post(\"/login\", (req, res) => {\r\n+    const { username, password } = req.body;\r\n+\r\n+    if(!username || !password) return res.json({ success: false, message: \"กรอกข้อมูลไม่ครบ\" });\r\n+\r\n+    db.get(`SELECT * FROM Users WHERE username = ?`, [username], async (err, row) => {\r\n+        if(err) return res.json({ success: false, message: err.message });\r\n+        if(!row) return res.json({ success: false, message: \"ชื่อผู้ใช้ไม่ถูกต้อง\" });\r\n+\r\n+        const match = await bcrypt.compare(password, row.passwordHash);\r\n+        if(match) res.json({ success: true, message: \"เข้าสู่ระบบสำเร็จ\" });\r\n+        else res.json({ success: false, message: \"รหัสผ่านไม่ถูกต้อง\" });\r\n+    });\r\n+});\r\n+\r\n+app.listen(PORT, () => console.log(`Server running on http://localhost:${PORT}`));\r\n"
                },
                {
                    "date": 1765179247139,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,65 @@\n+const express = require(\"express\");\r\n+const path = require(\"path\");\r\n+const fs = require(\"fs\");\r\n+const bcrypt = require(\"bcrypt\"); // สำหรับเข้ารหัสรหัสผ่าน\r\n+\r\n+const app = express();\r\n+app.use(express.json()); // อ่าน JSON body\r\n+app.use(express.static(__dirname)); \r\n+\r\n+const USERS_FILE = path.join(__dirname, \"users.json\");\r\n+\r\n+// หน้า index\r\n+app.get(\"/\", (req, res) => {\r\n+  res.sendFile(path.join(__dirname, \"index.html\"));\r\n+});\r\n+\r\n+// Register\r\n+app.post(\"/register\", async (req, res) => {\r\n+  const { username, password, fullname } = req.body;\r\n+  if (!username || !password || !fullname) {\r\n+    return res.json({ success: false, message: \"กรอกข้อมูลให้ครบทุกช่อง\" });\r\n+  }\r\n+\r\n+  // อ่านไฟล์ผู้ใช้\r\n+  let users = JSON.parse(fs.readFileSync(USERS_FILE));\r\n+\r\n+  // ตรวจสอบ username ซ้ำ\r\n+  if (users.find(u => u.username === username)) {\r\n+    return res.json({ success: false, message: \"Username นี้มีคนใช้แล้ว\" });\r\n+  }\r\n+\r\n+  // เข้ารหัส password\r\n+  const hash = await bcrypt.hash(password, 10);\r\n+\r\n+  // เพิ่มผู้ใช้ใหม่\r\n+  users.push({ username, passwordHash: hash, fullname });\r\n+  fs.writeFileSync(USERS_FILE, JSON.stringify(users, null, 2));\r\n+\r\n+  res.json({ success: true, message: \"สมัครสมาชิกเรียบร้อย\" });\r\n+});\r\n+\r\n+// Login\r\n+app.post(\"/login\", async (req, res) => {\r\n+  const { username, password } = req.body;\r\n+  if (!username || !password) {\r\n+    return res.json({ success: false, message: \"กรอกข้อมูลให้ครบทุกช่อง\" });\r\n+  }\r\n+\r\n+  let users = JSON.parse(fs.readFileSync(USERS_FILE));\r\n+  const user = users.find(u => u.username === username);\r\n+\r\n+  if (!user) {\r\n+    return res.json({ success: false, message: \"ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง\" });\r\n+  }\r\n+\r\n+  const match = await bcrypt.compare(password, user.passwordHash);\r\n+  if (!match) {\r\n+    return res.json({ success: false, message: \"ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง\" });\r\n+  }\r\n+\r\n+  res.json({ success: true, message: `ยินดีต้อนรับ ${user.fullname}` });\r\n+});\r\n+\r\n+// เริ่ม server\r\n+app.listen(3000, () => console.log(\"Server running on http://localhost:3000\"));\r\n"
                },
                {
                    "date": 1765180093503,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,13 +1,24 @@\n const express = require(\"express\");\r\n const path = require(\"path\");\r\n const fs = require(\"fs\");\r\n const bcrypt = require(\"bcrypt\"); // สำหรับเข้ารหัสรหัสผ่าน\r\n+const session = require(\"express-session\"); // <-- เพิ่ม\r\n \r\n const app = express();\r\n app.use(express.json()); // อ่าน JSON body\r\n app.use(express.static(__dirname)); \r\n \r\n+\r\n+// ตั้งค่า session\r\n+app.use(session({\r\n+  secret: \"mysecretkey\",    // เปลี่ยนเป็นรหัสลับของคุณ\r\n+  resave: false,\r\n+  saveUninitialized: true\r\n+}));\r\n+\r\n+\r\n+\r\n const USERS_FILE = path.join(__dirname, \"users.json\");\r\n \r\n // หน้า index\r\n app.get(\"/\", (req, res) => {\r\n@@ -57,9 +68,25 @@\n   if (!match) {\r\n     return res.json({ success: false, message: \"ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง\" });\r\n   }\r\n \r\n+\r\n+  // เซฟข้อมูล user ลง session\r\n+  req.session.user = { username: user.username, fullname: user.fullname };\r\n   res.json({ success: true, message: `ยินดีต้อนรับ ${user.fullname}` });\r\n });\r\n \r\n+// 🔹 Middleware ตรวจสอบ login\r\n+function requireLogin(req, res, next) {\r\n+  if (!req.session.user) {\r\n+    return res.redirect(\"/login.html\");\r\n+  }\r\n+  next();\r\n+}\r\n+\r\n+// 🔹 Route protected\r\n+app.get(\"/exercises/exercise1.html\", requireLogin, (req, res) => {\r\n+  res.sendFile(path.join(__dirname, \"exercises/exercise1.html\"));\r\n+});\r\n+\r\n // เริ่ม server\r\n app.listen(3000, () => console.log(\"Server running on http://localhost:3000\"));\r\n"
                },
                {
                    "date": 1765180218837,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -89,5 +89,5 @@\n   res.sendFile(filePath);\r\n });\r\n \r\n // เริ่ม server\r\n-app.listen(3000, ()\n\\ No newline at end of file\n+app.listen(3000, () => console.log(\"Server running on http://localhost:3000\"));\r\n"
                },
                {
                    "date": 1765181381535,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,102 @@\n+const express = require(\"express\");\r\n+const path = require(\"path\");\r\n+const fs = require(\"fs\");\r\n+const bcrypt = require(\"bcrypt\"); // สำหรับเข้ารหัสรหัสผ่าน\r\n+const session = require(\"express-session\"); // <-- เพิ่ม\r\n+\r\n+const app = express();\r\n+app.use(express.json()); // อ่าน JSON body\r\n+app.use(express.static(__dirname)); \r\n+\r\n+\r\n+// ตั้งค่า session\r\n+app.use(session({\r\n+  secret: \"mysecretkey\",    // เปลี่ยนเป็นรหัสลับของคุณ\r\n+  resave: false,\r\n+  saveUninitialized: true\r\n+}));\r\n+\r\n+\r\n+\r\n+const USERS_FILE = path.join(__dirname, \"users.json\");\r\n+\r\n+// หน้า index\r\n+app.get(\"/\", (req, res) => {\r\n+  res.sendFile(path.join(__dirname, \"index.html\"));\r\n+});\r\n+\r\n+// Register\r\n+app.post(\"/register\", async (req, res) => {\r\n+  const { username, password, fullname } = req.body;\r\n+  if (!username || !password || !fullname) {\r\n+    return res.json({ success: false, message: \"กรอกข้อมูลให้ครบทุกช่อง\" });\r\n+  }\r\n+\r\n+  // อ่านไฟล์ผู้ใช้\r\n+  let users = JSON.parse(fs.readFileSync(USERS_FILE));\r\n+\r\n+  // ตรวจสอบ username ซ้ำ\r\n+  if (users.find(u => u.username === username)) {\r\n+    return res.json({ success: false, message: \"Username นี้มีคนใช้แล้ว\" });\r\n+  }\r\n+\r\n+  // เข้ารหัส password\r\n+  const hash = await bcrypt.hash(password, 10);\r\n+\r\n+  // เพิ่มผู้ใช้ใหม่\r\n+  users.push({ username, passwordHash: hash, fullname });\r\n+  fs.writeFileSync(USERS_FILE, JSON.stringify(users, null, 2));\r\n+\r\n+  res.json({ success: true, message: \"สมัครสมาชิกเรียบร้อย\" });\r\n+});\r\n+\r\n+// Login\r\n+app.post(\"/login\", async (req, res) => {\r\n+  const { username, password } = req.body;\r\n+  if (!username || !password) {\r\n+    return res.json({ success: false, message: \"กรอกข้อมูลให้ครบทุกช่อง\" });\r\n+  }\r\n+\r\n+  let users = JSON.parse(fs.readFileSync(USERS_FILE));\r\n+  const user = users.find(u => u.username === username);\r\n+\r\n+  if (!user) {\r\n+    return res.json({ success: false, message: \"ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง\" });\r\n+  }\r\n+\r\n+  const match = await bcrypt.compare(password, user.passwordHash);\r\n+  if (!match) {\r\n+    return res.json({ success: false, message: \"ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง\" });\r\n+  }\r\n+\r\n+\r\n+  // เซฟข้อมูล user ลง session\r\n+  req.session.user = { username: user.username, fullname: user.fullname };\r\n+  res.json({ success: true, message: `ยินดีต้อนรับ ${user.fullname}` });\r\n+});\r\n+\r\n+// 🔹 Middleware ตรวจสอบ login\r\n+function requireLogin(req, res, next) {\r\n+  if (!req.session.user) {\r\n+    return res.redirect(\"/login.html\");\r\n+  }\r\n+  next();\r\n+}\r\n+\r\n+// ป้องกันทุกไฟล์ใน /exercises\r\n+app.get(\"/exercises/:file\", requireLogin, (req, res) => {\r\n+  const filePath = path.join(__dirname, \"exercises\", req.params.file);\r\n+  res.sendFile(filePath);\r\n+});\r\n+\r\n+app.get(\"/me\", (req, res) => {\r\n+    if (req.session && req.session.user) {\r\n+        res.json(req.session.user);\r\n+    } else {\r\n+        res.json({});\r\n+    }\r\n+});\r\n+\r\n+\r\n+// เริ่ม server\r\n+app.listen(3000, () => console.log(\"Server running on http://localhost:3000\"));\r\n"
                }
            ],
            "date": 1765159604317,
            "name": "Commit-0",
            "content": "const express = require(\"express\");\r\nconst path = require(\"path\");\r\nconst app = express();\r\n\r\napp.use(express.static(__dirname));  // เสิร์ฟทุกไฟล์ในโฟลเดอร์นี้\r\n\r\napp.get(\"/\", (req, res) => {\r\n  res.sendFile(path.join(__dirname, \"index.html\"));\r\n});\r\n\r\napp.listen(3000, () => console.log(\"Server running on http://localhost:3000\"));\r\n"
        }
    ]
}